# Vince 的 HiFi 笔记

- `index.html` + `styles.css` + `app.js`,零外部依赖,直接放进任意静态站点即可。
- 右上角「主题」可切换 亮 / 中性 / 暗 三种模式 × 五套配色(localStorage 记忆,默认跟随系统)。
- 全部图表为内联 SVG 模型仿真示意,颜色随主题联动;点击图表可放大。
- `vince-hifi-notes-preview.html` 为单文件合并版,仅用于本地快速预览。
