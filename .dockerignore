# 只排除「不该进构建上下文」的大件/噪声。基础设施/源文件(docker/、tools/、templates/、posts/ 等)
# 仍需进上下文供 build 阶段编译;build-site 的 COPY_EXCLUDE 决定最终哪些进 /out 对外发布。
# 注意:posts-manifest.json 必须进上下文(CI 生成的发布判定),别在这里排除它。
.git
.DS_Store
**/.DS_Store
**/node_modules
/site
# demo 源料:11MB 研究文件,COPY_EXCLUDE 已挡住不对外发布,也不需要进构建上下文
/reactor-study-src
