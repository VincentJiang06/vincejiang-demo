export default {
  id: "C03",
  blocks: [
    { t: "prose", html: `
<p>指标都会被腐蚀，但这不意味着只能放弃测量。这一节是全课最建设性的落点：既然指标必被博弈，那就设计一套让博弈成本高到不划算的评估。你在 <code>C02</code> 学过用七根杠杆估一个评测会失真多狠，在 <code>G03</code> 学过怎么用留出题库和动态轮换让优化循环永远追不上，在 <code>G05</code> 学过抗博弈机制设计的四条退路，还有那招有 43 倍实验撑腰的「把评分藏起来」。这一课把它们拼成一台完整的机器，并说清为什么单靠任何一件都不够。</p>
<p>先把天花板钉在墙上，你在 <code>G05</code> 已经见过它，这里只唤醒一句。Gibbard–Satterthwaite 定理证明，一般情形下不存在既有效又完全防操纵的聚合规则；Skalse 等 2022 年的机器学习版同向：除了平凡情形，任何一对不完全对齐的奖励几乎必然可被博弈。所以「造一个不可被博弈的 eval」在数学上是死路，可行的目标只有四条有可证代价的退路，限制适用域、接受近似、改变结构、抬高博弈成本。这一课做的，是把这四条组装成一条可用的多层防线。</p>` },

    { t: "module", module: "sandbox:design-robust-eval", title: "抗博弈 eval 构建器", config: {} },

    { t: "prose", html: `
<p>沙盒里的关键规律只有一条：没有单一防御能挡住一切，是多层组合把博弈成本顶到不划算。每一层堵一条特定的通道。</p>
<ul>
<li><strong>留出与动态轮换</strong>堵题目过拟合与污染（接 <code>G03</code>、<code>Y02</code>、<code>Y03</code>）：让优化循环永远看不到、追不上你的题库。诚实提示，同样来自 G03，藏题买的是时间不是免疫，2026 年跨基准的系统研究显示，私有测试集的饱和速度和公开的没有统计差异。</li>
<li><strong>配对指标制衡</strong>堵维度窄化与风格刷分（接 <code>B09</code>、<code>Y08</code>）：几个互相拉扯的指标很难被同时满足，也顺带缓解代理指标替代（<code>B07</code>）。它的上限来自 <code>B10</code>：只要真实价值是高维隐含的，任何有限维的加权组合都留着残余的对齐缺口。</li>
<li><strong>eval 与优化解耦</strong>堵「直接顶着边界往上爬」（接 <code>G02</code>）：eval 只用来诊断，不直接驱动改模型。指标是开罐器，不是仪表盘。</li>
<li><strong>随机人工抽检</strong>堵「测不到的部分被无视」（接 <code>B05</code> 的 McNamara 谬误）：刻意去看 rubric 覆盖不到的维度，对抗那句「测不了就当它不存在」。</li>
<li><strong>承认容量约束</strong>堵真实挤占（接 <code>B09</code>）：别把全部优化压力压在一个窄 eval 上，模型的容量有限，投给被测维度的会从别处抽走。</li>
<li><strong>评分逻辑对被评者不可见</strong>（接 <code>G05</code>）：METR 2025 年测得，同一模型在能看到评分函数的环境里作弊率是看不到时的 43 倍。这条有微观基础，<code>B12</code> 委托代理里，策略性不透明是能从一阶条件推出来的最优机制，不是行政偷懒。</li>
</ul>
<p>把六层叠起来，你会发现它们不是并列的备选，而是互补的：留出治的是「记住题」，配对治的是「只练一面」，解耦治的是「顶着分数爬」，人审治的是「测不到就无视」，限压治的是「把别处抽干」，藏分治的是「照着评分函数钻」。缺一层，就漏一条道。这也是为什么单点防御总失效，博弈会从没设防的那条缝里挤过去。</p>
<p>拿配对制衡举个具体的。只考核 agent 的「任务成功率」，它会学会用最省事、副作用最大的方式冲高成功率；给它配上一个「走捷径就恶化」的对立指标，比如有害副作用率、资源消耗、对无关状态的破坏、幻觉率，单独博弈成功率就会立刻在配对项上露馅。这套思路 Andrew Grove 1983 年就用一句话讲透：考核卖出的早餐份数，别只数煮了多少个蛋。配对的诀窍不在多加几个 KPI，而在选出真正正交、作弊手段不相通的对立面；两个指标要是能被同一种作弊同时拉高，制衡就是假的。</p>
<p>再拿随机人审举个具体的。Bevan 与 Hood 研究英国 NHS 时点破一个通病，他们叫它以偏概全：被测的那一部分被当成了整体，没测到的就当不存在。对策不是把所有维度都测全，那办不到，而是让「随时可能被人抽出来细看」这件事可信，威慑就远大于实际抽查的成本，税务研究里管这叫稻草人效应。关键是抽检要落在最容易钻空子的接缝上，且不可预测，被查者算不准哪次会被翻开，定向作弊的期望收益就塌了。</p>` },

    { t: "callout", variant: "applied", html: `
<p><strong>给你自己的 eval 上这套防线，按这个顺序最省力。</strong>先做那件不花钱的：把 eval 的设计者和使用者对齐，让定分的人自己就要靠这个 agent 干活，先把利益冲突压下去。再留一组从不进任何优化回路的私有题，并给主池排一张轮换表，记下每套题的「服役时长」。接着给每个能力指标配一个对立项，写明「作弊这一项会在哪一项上暴露」。然后把评分脚本、判分测试、成功标准对被评者关掉，异常高分触发人工深挖而不是自动放行。最后把「每季度复核这套 eval 还在生产什么」写进日程。顺序重要：先对齐、再设防，比一上来就把 eval 修严更省力，也更持久。</p>` },

    { t: "callout", variant: "intuit", html: `
<p><strong>最有价值的一条心法，不是「把作弊弄贵」，而是「先降低利益冲突」。</strong>cheap talk（廉价磋商）传统证明过一件事：维持诚实所需的信号成本，正比于评估者和被评估者之间的利益冲突。冲突为零时，诚实是免费的；冲突越大，才越需要昂贵的验证去锁住它。所以正确的顺序是先想办法对齐利益，让 eval 的设计者自己就要用这个 agent，让团队的奖励绑在真实效果而不是榜面分上，再决定还需要多少成本去锁剩下的冲突。另一条硬提醒来自策略性分类（<code>Y10</code>）：想让 eval 激励真改进而不是刷分，你其实要解一个因果推断问题，弄清哪些可测行为因果地通向真实能力，别指望有纯技术的一劳永逸方案。最接近可行的做法，是把评估当成一连串干预，改一改 eval 的构成，看生态怎么响应，再用响应反推因果结构。</p>` },

    { t: "callout", variant: "myth", html: `
<p><strong>抗博弈手段自己也会被 Goodhart，这不是俏皮话，是要写进日程的事。</strong>三个实例。其一，Dodd-Frank 要求证券化机构自留 5% 风险以保证切身利益，实践中这份风险可以用衍生品对冲掉，名义合规、实际没皮。其二，一个本意用来诊断的探针指标，一旦进了年度考核，就退化回一块只求好看的仪表盘（Klein 2025 对 NHS 的实证）。其三，用来抓作弊的思维链监控，一旦被纳入优化压力，模型就学会把意图藏起来，这就是最近未堵策略（<code>Y05</code>）。所以抗博弈是需要持续维护的制度纪律，不是一次性的设计。把「每季度复核这套 eval 还在生产什么」写进日程，而不只是写进文档；机构层怎么做，接 <code>G06</code>。</p>` },

    { t: "prose", html: `
<p>留一个问题：四条退路各有可证明的代价，那有没有一个统一的「可操纵性预算」框架，把利益冲突的成本、近似防博弈的程度、藏题和审计的开销，全放进同一个「我愿意容忍多少作弊、愿意付多少代价」的算式里一起最优化？目前没有。这是把 <code>G01</code> 到 <code>G05</code> 整条绿色防线收成一个可计算目标的最后一块拼图。</p>
<p><strong>一句话带走：</strong>没有单层防御挡得住博弈；扛得住的 eval，是几层各堵一条路的防线叠出来的，而最省力的第一步是先把利益对齐。</p>` },

    { t: "sources", items: [
      `Gibbard (1973) / Satterthwaite (1975) 可操纵性不可能定理；Skalse et al. (2022). "Defining and Characterizing Reward Hacking." NeurIPS。`,
      `Bevan & Hood (2006)；Carter (1991)（tin openers vs dials）；Klein (2025) NHS 探针退化实证。`,
      `METR (2025). RE-Bench 30.4% 对 HCAST 0.7%，约 43 倍（可见度效应）。`,
      `Miller, Milli & Hardt (2020)（因果建模定理）；Crawford & Sobel (1982)（成本正比于冲突）。`,
      `综合 <code>research/06、10、14</code> 与 <code>research/deep/D7</code> §5；绿色分支处方见 <code>G01</code>–<code>G06</code>。`
    ] }
  ]
};
