export default {
  id: "C03",
  blocks: [
    { t: "prose", html: `
<p>Every metric corrodes, but that does not mean the only option is to stop measuring. Since any metric will be gamed, design an evaluation where gaming costs more than it is worth. In <code>C02</code> you learned to use the seven levers to estimate how badly an evaluation will distort, in <code>G03</code> how held-out test sets and dynamic rotation keep the optimization loop permanently behind, in <code>G05</code> the four fallbacks of strategyproof mechanism design, plus the trick with 43x experimental backing: hide the scoring. Assembling them into one working machine matters, because no single piece is enough.</p>
<p>Nail the ceiling to the wall first. You saw it in <code>G05</code>: the Gibbard-Satterthwaite theorem proves that in the general case no aggregation rule is both effective and fully manipulation-proof; the machine learning version from Skalse et al. in 2022 points the same way, that outside trivial cases any pair of imperfectly aligned rewards is almost certain to be gameable. So "build an ungameable eval" is mathematically a dead end, and the only feasible goals are the four fallbacks with provable costs: restrict the domain, accept approximation, change the structure, raise the cost of gaming. What remains is to assemble those four into a usable layered defense.</p>` },

    { t: "module", module: "sandbox:design-robust-eval", title: "Game-resistant eval builder", config: {} },

    { t: "prose", html: `
<p>There is only one key regularity in the sandbox: no single defense stops everything, and it is the combination of layers that pushes the cost of gaming past worth it. Each layer blocks one specific route.</p>
<ul>
<li><strong>Held-out sets and dynamic rotation</strong> block item overfitting and contamination (follows <code>G03</code>, <code>Y02</code>, <code>Y03</code>): keep the optimization loop from ever seeing or catching up with your item pool. An honest caveat, also from G03: hiding items buys time, not immunity. A 2026 cross-benchmark study found no statistical difference between the saturation rates of private and public test sets.</li>
<li><strong>Paired counterweight metrics</strong> block dimensional narrowing and style-driven score inflation (follows <code>B09</code>, <code>Y08</code>): several metrics pulling against each other are hard to satisfy at once, and it also eases surrogation (<code>B07</code>). Its ceiling comes from <code>B10</code>: as long as true value is high-dimensional and implicit, any finite-dimensional weighted combination leaves a residual alignment gap.</li>
<li><strong>Decoupling eval from optimization</strong> blocks climbing straight up against the boundary (follows <code>G02</code>): the eval is only for diagnosis and does not directly drive model changes. A metric is a tin opener, not a dial.</li>
<li><strong>Random human spot checks</strong> block "whatever cannot be measured gets ignored" (follows the McNamara fallacy in <code>B05</code>): deliberately look at dimensions the rubric does not cover, against the line that if it cannot be measured it does not exist.</li>
<li><strong>Acknowledging capacity constraints</strong> blocks real crowding-out (follows <code>B09</code>): do not put all the optimization pressure on one narrow eval. Model capacity is finite, and what goes to the measured dimension is drawn from somewhere else.</li>
<li><strong>Scoring logic invisible to the evaluated</strong> (follows <code>G05</code>): METR measured in 2025 that the same model cheats 43 times as often in environments where it can see the scoring function as where it cannot. This one has a microfoundation: in the principal-agent setting of <code>B12</code>, strategic opacity is an optimal mechanism derivable from first-order conditions, not administrative laziness.</li>
</ul>
<p>Stack the six and you find they are not alternatives but complements: held-out treats "memorizing the items," pairing treats "training only one side," decoupling treats "climbing the score," human review treats "ignoring what is not measured," pressure limits treat "draining everywhere else," and hidden scoring treats "drilling into the scoring function." Drop a layer and you open a route. Which is why single-point defenses always fail. Gaming squeezes through whichever seam is unguarded.</p>
<p>Take paired counterweights concretely. Assess an agent on task success rate alone and it will learn to hit high success by the laziest route with the worst side effects. Pair it with an opposing metric that gets worse when it takes shortcuts, such as harmful side-effect rate, resource consumption, damage to unrelated state, or hallucination rate, and gaming success rate alone immediately shows up on the paired item. Andrew Grove made the point in one line back in 1983: measure breakfasts served, not eggs cooked. The trick of pairing is not adding a few more KPIs, it is picking opposites that are genuinely orthogonal, where the cheats do not carry over. If one cheat can raise both metrics at once, the counterweight is fake.</p>
<p>Take random human review concretely. Studying the English NHS, Bevan and Hood named a common failure they called synecdoche: the part that is measured gets treated as the whole, and what is not measured is treated as nonexistent. The remedy is not to measure every dimension, which cannot be done, but to make it credible that anything might at any moment be pulled out for close reading. The deterrent then far exceeds the cost of the actual sampling, which tax research calls the scarecrow effect. The key is that checks land on the seams most open to exploitation and stay unpredictable. If the subject cannot work out which round will be opened up, the expected payoff of targeted cheating collapses.</p>` },

    { t: "callout", variant: "applied", html: `
<p><strong>To put this defense on your own eval, this order costs the least effort.</strong> Start with the free one: align the eval's designers with its users, so the people setting the scores have to rely on this agent for their own work, which pushes the conflict of interest down first. Then hold back a private set of items that never enters any optimization loop, and draw up a rotation schedule for the main pool, recording each set's time in service. Next, pair every capability metric with an opposing item, and write down where cheating on the first will show up on the second. Then close the scoring scripts, judging tests and success criteria to the evaluated party, and make anomalously high scores trigger a manual deep dive rather than an automatic pass. Finally, put "review quarterly what this eval is actually producing" on the calendar. Order matters: aligning first and defending second takes less effort and lasts longer than tightening the eval right out of the gate.</p>` },

    { t: "callout", variant: "intuit", html: `
<p><strong>The single most valuable principle is not "make cheating expensive," it is "lower the conflict of interest first."</strong> The cheap talk tradition proved one thing: the signaling cost required to sustain honesty is proportional to the conflict of interest between evaluator and evaluated. At zero conflict, honesty is free; the larger the conflict, the more expensive verification you need to lock it in. So the right order is to align interests first, so the eval's designers have to use this agent themselves and the team's rewards are tied to real effect rather than leaderboard score, and only then decide how much cost is needed to lock down the remaining conflict. Another hard reminder comes from strategic classification (<code>Y10</code>): to make an eval incentivize real improvement rather than score chasing, you actually have to solve a causal inference problem, working out which measurable behaviors causally lead to real capability, and there is no purely technical one-and-done solution. The closest thing to workable is to treat evaluation as a series of interventions, change the eval's composition a little, watch how the ecosystem responds, and use the response to infer the causal structure.</p>` },

    { t: "callout", variant: "myth", html: `
<p><strong>Anti-gaming measures get Goodharted themselves, and that is not a quip, it is something to put on the calendar.</strong> Three instances. First, Dodd-Frank required securitizers to retain 5% of the risk to keep skin in the game, and in practice that risk can be hedged away with derivatives: nominally compliant, no skin left. Second, a probe metric intended for diagnosis, once it enters the annual appraisal, degrades back into a dial that only has to look good (Klein's 2025 NHS evidence). Third, chain-of-thought monitoring used to catch cheating: once it is brought under optimization pressure, the model learns to hide its intent, which is the recent unblocked-strategy problem (<code>Y05</code>). So anti-gaming is institutional discipline requiring continuous maintenance, not a one-off design. Put "review quarterly what this eval is actually producing" on the calendar, not just in a document. For how to do this at the institutional level, see <code>G06</code>.</p>` },

    { t: "prose", html: `
<p>One open question: the four fallbacks each carry a provable cost, so is there a unified "manipulability budget" framework that puts the cost of conflict of interest, the degree of approximate strategyproofness, and the overhead of hidden items and audits into one calculation of how much cheating I am willing to tolerate and how much I am willing to pay, and optimizes them together? Not yet. It is the last piece needed to collapse the whole green defensive line from <code>G01</code> to <code>G05</code> into a single computable objective.</p>
<p><strong>The one-line takeaway:</strong> no single layer stops gaming; an eval that holds up is several layers stacked, each blocking one route, and the cheapest first step is to align interests.</p>` },

    { t: "sources", items: [
      `Gibbard (1973) / Satterthwaite (1975) manipulability impossibility theorem; Skalse et al. (2022). "Defining and Characterizing Reward Hacking." NeurIPS.`,
      `Bevan & Hood (2006); Carter (1991) (tin openers vs dials); Klein (2025), evidence on NHS probe degradation.`,
      `METR (2025). RE-Bench 30.4% vs HCAST 0.7%, about 43x (visibility effect).`,
      `Miller, Milli & Hardt (2020) (causal modeling theorem); Crawford & Sobel (1982) (cost proportional to conflict).`,
      `Synthesized from <code>research/06、10、14</code> and <code>research/deep/D7</code> §5; green-branch prescriptions in <code>G01</code>–<code>G06</code>.`
    ] }
  ]
};
