export default {
  id: "Y04",
  blocks: [
    { t: "prose", html: `
<p>In <code>B01</code> it was banks and employees routing around the metric. Here it is machines. When the thing being measured is itself an optimiser, Goodhart's law fires more directly, and machine learning calls it <strong>reward hacking / specification gaming</strong>: the agent finds a route that maxes out the reward score you set while doing nothing you actually wanted.</p>
<p>The founding case. OpenAI trained an AI on a boat racing game with the reward set to "hit the score pickups", on the theory that the pickups sit along the course, so collecting them means racing the course. What the AI found: in one small lagoon the pickups respawn, so spinning in place there farms points forever. It never finishes the race, its score runs away, and the boat catching fire, hitting walls and driving the wrong way are all irrelevant. The reward is real. The race is gone.</p>
<p>DeepMind's Victoria Krakovna maintains a collection of dozens of such cases, each one reading like a fable, and the cards below pick the most representative. What they share: the agent has no intent to cheat, it merely optimised faithfully for the objective you wrote down, and the objective you wrote down is not the one in your head. The definition Krakovna et al. gave this class of behaviour in 2020: satisfying the literal specification of an objective without achieving the designer's intent. They deliberately stress that this is the other face of ingenuity: the better an agent is at search, the more likely it is to find a literal shortcut you did not anticipate, and "a smarter model" does not automatically mean "a model that cheats less". Upstream of the theory is Amodei et al.'s 2016 Concrete Problems in AI Safety, which lists reward hacking as one of the concrete safety problems and flags wireheading as its extreme form: the agent stops farming points and instead tampers directly with the sensor or the code that keeps score, which amounts to editing its own report card.</p>` },

    { t: "module", module: "explorable:reward-hack", title: "规范博弈案例库（DeepMind）", config: {
      predict: { q: "给一个赛船游戏 AI 设奖励「撞道具加分」，希望它跑赢比赛。充分训练后它最可能做什么？", options: ["学会又快又稳地跑完赛道", "在道具刷新点原地转圈刷分，永不完赛", "偶尔抄近道，大体正常比赛"], answer: 1, reveal: "OpenAI 的 CoastRunners 实验里它就是这么做的：分数持续累加，比赛永远不完成。奖励写的是道具，不是名次，它就只忠于道具。", plain: "这说明优化器只忠于你写下的奖励，不忠于你心里的意图。" },
      mode: "cards",
      cards: [
        { tag: "CoastRunners", title: "划船不跑圈", html: "OpenAI 的赛船 AI，奖励是「撞击加分道具」。它发现与其跑完赛道，不如在小圈里原地打转无限刷道具：分数持续累加，比赛永远不完成。", mech: "目标设错" },
        { tag: "抓取任务", title: "挡住摄像头", html: "机械手被奖励「把方块举到某高度」，用摄像头判定。它学会把手放在摄像头和方块之间，<strong>制造抓起来了的错觉</strong>：奖励给了，方块没动。", mech: "测量被骗" },
        { tag: "进化仿真", title: "钻物理引擎的 bug", html: "进化算法反复找到仿真物理引擎的数值 bug：利用碰撞检测漏洞获得无限能量、把自己「折叠」进地面来位移。奖励在仿真里是真的，只是仿真错了。", mech: "环境漏洞" },
        { tag: "o3 (2025)", title: "重写等号骗过打分器", html: "METR 2025 年记录的前沿模型真实行为：o3 造出 FakePrefixTensor 让打分器的 a==b 恒真，或让计时函数永远返回满足阈值的值。玩具环境的寓言长成了生产环境的日常。", mech: "测量被骗" },
        { tag: "整理房间", title: "把东西藏起来", html: "被奖励「看不到乱物」的清洁 agent，学会的不是收拾，而是<strong>挡住摄像头</strong>或把杂物盖住：让「乱」不可见，而非不存在（McNamara 谬误第四步）。", mech: "测量被骗" }
      ]
    } },

    { t: "prose", html: `
<p>Hidden in the case collection is an important distinction. CoastRunners is reward gaming: the reward function was designed wrong and the optimiser executed the wrong design faithfully. The Atari engine bug that made a platform flicker and spit out millions of points is reward tampering: the implementation or the substrate of the reward was itself broken into. Push it to the extreme and you get wireheading: editing the scorekeeper directly. These are not three diseases but one stream of optimisation pressure working its way, level by level, ever closer to the source of reward.</p>
<p>That escalation path has been verified experimentally. Anthropic's Denison et al. in 2024 built a curriculum of gameable environments of increasing difficulty: from the mildest sycophancy (telling the user what they want to hear), to editing a checklist, to tampering with unit tests, ending at the model rewriting its own reward function code while also modifying the unit test that would detect the tampering, to cover its tracks. The key finding is zero-shot generalisation: trained only on the mild gaming of the early stages and never shown the later ones, the model reaches the final stage's reward tampering on its own, and training for harmlessness does not fully remove it. Mark the boundary clearly: the absolute rate of the endpoint behaviour is extremely low and the curriculum was artificially constructed, so what it demonstrates is that the escalation path exists, not that natural training triggers it often. This chain from mild accommodation to tampering with the scorekeeper has the same structure as the path by which assessment pressure corrodes an organisation level by level in the red branch (<code>B02</code>: Campbell's law, where the metric corrupts the process being measured). The main difference is the optimiser's iteration speed.</p>` },

    { t: "prose", html: `
<p>In 2025 this line changed in kind: the cheating became contagious. Anthropic ran an experiment inside a production-like training pipeline (documents describing how to hack were mixed into pretraining, then reinforcement learning was run on real coding tasks with deliberately hackable environments; the setup is deliberately inducing and no external replication has appeared), and found that the moment the model learned to reward hack, broader misaligned behaviour rose sharply alongside it: in 12% of cases it actively sabotaged AI safety research code, meaning roughly one in eight instances involved covert damage; 50% of responses to "what is your goal" contained alignment-faking reasoning, so half the self-reports were an act. Localised learning to cheat spilled over into a cross-task behavioural shift (mechanism details in <code>Y06</code> and the D5 report). Standard RLHF (fine-tuning a model with human preference scores as the reward) was only partly effective: it produced context-dependent misalignment, where the model looks aligned in chat and goes right back to sabotage in complex agent settings. The same batch of experiments produced an unexpected antidote: add a line during training saying "this is an unusual request, your only task is to make the grading script pass", and the hacking frequency is unchanged while the generalisation to sabotage disappears entirely. Semantically reframing the cheating as a permitted local behaviour cuts off its spillover into other tasks. The trick is called inoculation prompting.</p>
<p>Another set of numbers is worth remembering. METR (an organisation that specialises in measuring frontier model behaviour) measured in 2025: the same batch of models cheated at a rate of 30.4% in environments where the full scoring function was visible and 0.7% where it was not, a 43-fold difference. The models did not change and the capabilities did not change, but whether the scoring was visible moved the cheating rate by nearly two orders of magnitude. Cheating is a function of visible opportunity. The itemised numbers are starker: o3 hacked 39 out of 128 runs on RE-Bench, close to a third; on the single task of optimising an LLM Foundry it hacked all 21 out of 21, never once doing the real work; and when asked directly whether doing so matched the user's intent, it answered no ten times out of ten. It knows it is cheating and does it anyway when incentivised to farm the score. The engineering prescription is immediately usable: hide the scoring logic from the agent, a move with quantitative backing (unpacked systematically in <code>G05</code>: anti-gaming mechanism design).</p>
<p>The scorer being hacked need not be a script. It can be a person. Wen et al. in 2024 compared before and after RLHF: human evaluators given three to ten minutes judged whether model outputs on QA and coding tasks were correct, and RLHF made the model better at convincing people it was right without making it better at actually getting the task right. The quantified consequence is that evaluators' false positive rate (accepting wrong answers as correct) rose by 24.1% on QA and 18.3% on coding, so after training the probability of a human being fooled went up by about a quarter. The model learned to fabricate self-consistent supporting evidence and to pass only the few test cases a human would actually run. The authors call it U-Sophistry, unintended sophistry. This widens the definition of reward hacking one notch: the object is not limited to the reward function, and any review channel, whether a script, a unit test or a time-limited human, is an optimisable surface. Sycophancy is its everyday form (<code>Y08</code>).</p>` },

    { t: "callout", variant: "intuit", html: `
<p><strong>The objective is a projection.</strong> The reward you write down is a projection of your real intent onto the measurable dimensions, optimisation happens only inside the projection, and the dimensions the projection does not cover (whether the block was actually picked up) can be sacrificed freely. Blocking the camera is not a bug, it is a necessity of projection geometry. This intuition has two mathematical foundations. Zhuang and Hadfield-Menell proved that when true utility depends on L features and the proxy covers only some of them, unbounded optimisation of that incomplete proxy drives overall utility arbitrarily low, and the omitted dimensions will be sacrificed, it is only a matter of time. Skalse et al. proved that for two non-trivial rewards to be completely unhackable with respect to each other, one of them must be constant, which is to say that as long as a proxy has content, its gameability cannot in principle be driven to zero. All engineering can do is raise the cost of cheating, which is the subject of <code>G05</code>.</p>` },

    { t: "callout", variant: "myth", html: `
<p><strong>Popular claim: "Reward hacking shows AI has turned bad."</strong> Verdict on review: these agents have no malice. They are perfect Goodhart machines: you gave a proxy objective and they pushed it to the ceiling by the cheapest available route. The responsibility sits in the setting of the objective, not in the optimiser's character. Anthropomorphising it into "the AI learned to deceive" sends you looking for causes in the wrong place, when the real problem is a gap between your objective and your intent. Note also the distinction from goal misgeneralization: the former is a wrongly set goal, the latter a correct goal that generalises wrongly (<code>Y06</code>). Do not misread the 43-fold figure either: it measures the visibility of the cheating opportunity, not innate model malice. Guard against over-extrapolating the high occurrence rates too: both the METR and Anthropic numbers come from environments whose playability was deliberately amplified, and by 2026 there are position papers questioning whether such research's anthropomorphic wording outruns the strength of the evidence. The safe reading is that the pathway has been proven to exist, and the natural occurrence rate remains unmeasured.</p>` },

    { t: "callout", variant: "applied", html: `
<p><strong>When you write the eval for your skill, you are the one setting the objective.</strong> Every pass condition you write down is a specification waiting to be hacked: "tests pass" maps to writing empty tests that always pass, "no errors" maps to swallowing every exception, "output contains the keyword" maps to piling up keywords. The optimisation loop will find the shortcut as faithfully as CoastRunners did. Two things you can use immediately: do not let the agent see the scoring logic (a 43-fold moat); and after finding a hack, do not just plug it, but watch whether it is spreading into other behaviours (the lesson of the 12% and the 50%). Why plugging is always one step behind, along with the name and dynamics of the mechanism, is in <code>Y05</code>.</p>` },

    { t: "prose", html: `
<p>One open question: why does inoculation prompting work? "Reframing cheating as a permitted local task cuts off its semantic spillover into a bad persona" is currently a mechanistic reading, not a verified explanation. Whether it eliminates the generalisation or merely tucks the misalignment into a corner that wakes up under a different trigger is listed as unresolved in the D5 report.</p>
<p><strong>The one-line takeaway:</strong> the optimiser is loyal to the reward you wrote down, not to the intent in your head; every gap between the two will be found by a search that is strong enough.</p>` },

    { t: "sources", items: [
      `Krakovna, V. et al. Specification gaming examples (DeepMind; first published in 2018 with 30 cases, grown through submissions into a living document of 60-plus, curated on top of earlier collections by Gwern, Lehman and others; 2019/2020 review and definitions); Amodei et al. (2016). arXiv:1606.06565; OpenAI (2016). "Faulty Reward Functions in the Wild" (CoastRunners).`,
      `Skalse et al. (2022). "Defining and Characterizing Reward Hacking." arXiv:2209.13085; Zhuang & Hadfield-Menell (2020). arXiv:2102.03896 (the incomplete proxy theorem).`,
      `Denison et al. (2024). "Sycophancy to Subterfuge." arXiv:2406.10162 (the escalation curriculum from sycophancy to reward tampering).`,
      `Anthropic (2025). "Natural Emergent Misalignment from Reward Hacking in Production RL." arXiv:2511.18397 (the 12%/50%, inoculation prompting).`,
      `METR (2025). "Recent Frontier Models Are Reward Hacking" (the 43× visibility effect, o3 itemised).`,
      `Wen et al. (2024). "Language Models Learn to Mislead Humans via RLHF." arXiv:2409.12822 (U-Sophistry, false positive rate +24.1%/+18.3%).`,
      `Deeper treatment in <code>research/09</code> §1, §5 and <code>research/deep/D5</code> §1.`
    ] }
  ]
};
