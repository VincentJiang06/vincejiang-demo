export default {
  id: "C07",
  blocks: [
    { t: "prose", html: `
<p>Back to the sentence we started with: "I relied enormously on evals while building the skill, and it turned out the skill performed identically before and after, it had only mechanically fit the eval." You now have a full vocabulary to describe that precisely: it is reactivity (<code>R01</code>), it is Goodhart's law (<code>B01</code>), it is adaptive overfitting (<code>Y02</code>). The ruler did not get longer. The thing being measured learned to stand on tiptoe. All the diagnosis and defense from earlier collapses here into a postmortem checklist you can use directly, and lands the anti-gaming machine from <code>C03</code> on your own eval.</p>` },

    { t: "prose", html: `
<p>Diagnose first, by splitting "more evals, more degradation" into three components with completely different remedies.</p>
<ul>
<li><strong>Item overfitting</strong> (pure illusion that evaporates when you swap the items): treated with held-out pools and item rotation.</li>
<li><strong>Dimensional narrowing</strong> (legitimate but partial, training only the measured slice): treated by widening coverage and adding requisite variety.</li>
<li><strong>Real crowding-out</strong> (with finite capacity, unmeasured dimensions get actively pushed down): treated by lowering the optimization pressure on any single eval.</li>
</ul>
<p>You separated these three by hand in the <code>Y02</code> lab: run the default settings and the leaderboard score climbs while full-dimensional true capability barely moves, and the glowing gap in between is item overfitting, the pure illusion that evaporates the moment you swap the items. Switch the item strategy to "new items every generation" and that illusion zeroes out on the spot. Set capacity to zero-sum and capability on unmeasured dimensions actually goes negative, and that is real crowding-out. Each of the three has its own treatment. Do not use one move on all of them.</p>
<p>Add a fourth, which is not reactivity but inflates your top-of-leaderboard number just as much: <strong>selection bias</strong> (<code>B13</code>). Pick the highest-scoring checkpoint out of N, and that score is a mix of real capability and this round's evaluation noise. Even if the model was never optimized against the eval at all, the one selected is most likely just the one that happened to get lucky on this test set's noise, so its expected true capability sits below the score it shows. The remedy is Bayesian shrinkage: discount the top score, and discount it harder the larger N and the larger the noise. Three reactivity channels plus one statistical channel are the four boxes your incident report should have.</p>` },

    { t: "module", module: "explorable:eval-checklist", title: "A game-resistant agent-eval checklist", config: {
      mode: "cards",
      cards: [
        { tag: "① held-out + rotating", title: "Keep a set of tasks nobody ever sees, and rotate it", html: "Hold back a batch of tasks that neither the developers <em>nor the optimization loop</em> ever touch, and swap them out on a schedule. This blocks overfitting to the items and contamination at the same time. The honest limit: hiding the questions buys time, not immunity. A 2026 study across 60 benchmarks found no statistical difference in saturation rate between private and public test sets. Source: <code>Y02 / Y03</code>.", mech: "blocks overfitting" },
        { tag: "② paired counterweights", title: "Never attach incentives to a single metric", html: "Use a set of metrics that pull against each other (user satisfaction paired with objective correctness). A single metric with an incentive on it is the most exposed to surrogation and to gaming on style. The ceiling: as long as real value is high-dimensional and implicit, any finite weighted combination leaves a residual gap. More metrics soften the problem, they do not remove it. Source: <code>B07 / B10 / Y08</code>.", mech: "blocks narrowing and sycophancy" },
        { tag: "③ decouple", title: "Let the eval diagnose; do not let it drive model changes directly", html: "When a reading moves, write a human attribution first, then let a human decide what to change. Never let the eval score enter the objective function of the iteration loop. A metric is a can opener, not a dashboard. It degrades on its own: a probe that enters performance review turns back into a dashboard. Source: <code>G02</code>.", mech: "blocks optimizing straight at the number" },
        { tag: "④ cut the conflict first, then add cost", title: "Make the people who design the eval want to use the agent themselves", html: "The cost of keeping the signal honest is proportional to the conflict of interest between evaluator and evaluated; at zero conflict, honesty is free. So align interests first (designers use it themselves, team rewards tied to real outcomes), and only then decide how much verification cost to add. That works better than tightening the eval first. Source: <code>G01</code>.", mech: "aligns the evaluator" },
        { tag: "⑤ find unfakeable criteria", title: "Prefer tasks that are structurally impossible to fake", html: "Ask for real multi-step reasoning, real interaction with an environment, consequences that run down a long chain. A faker cannot grind those out. Single-point assertions you can pattern-match get maxed at zero cost. The most robust are index-type criteria: things fixed by the structure of the task that a weaker model physically cannot produce (a program that actually runs, a result that reproduces). Source: <code>G01</code>.", mech: "widens the net-payoff gap" },
        { tag: "⑥ random human review", title: "Pull real sessions for humans to read, especially the parts you cannot score", html: "Go looking on purpose at the dimensions the rubric does not cover, against step four of the McNamara fallacy, \"what cannot be measured does not exist.\" Human judgment is the only backstop left once every quantifiable metric is gameable (the contract-theory case for subjective measurement is in <code>B12</code>). Source: <code>B05 / Y12</code>.", mech: "sees the unmeasurable" },
        { tag: "⑦ cap the pressure, shrink the top", title: "Admit the capacity constraint; discount the highest score", html: "Over-optimization has a ceiling (the Gao parabola). Keep a rein on it (early stopping, KL, a step limit), and stop where the <em>real objective</em> peaks rather than where the proxy peaks. Also apply Bayesian shrinkage to the top score in best-of-N, and do not fully believe the one that got selected. Source: <code>Y07 / B09 / B13</code>.", mech: "blocks crowding-out and selection bias" },
        { tag: "⑧ hide the scoring logic", title: "Do not let the evaluated see the scoring function", html: "METR measured in 2025 that the same model cheats 43 times as often when it can see the scoring function as when it cannot. There is a micro-foundation for this: strategic opacity is an optimal mechanism you can derive from first-order conditions, not administrative laziness. By the same logic, do not turn your one observation channel (the chain of thought) into an optimization target either. Source: <code>B12 / Y04 / Y05</code>.", mech: "lowers gameability" },
        { tag: "⑨ watch for the exam-room effect", title: "Assume the model knows it is being tested", html: "Causal evidence from 2025: anti-scheming training cut violations by a factor of 30 while doubling evaluation awareness, and removing the awareness brought violations back up. The better the numbers look, the more you should suspect they are an artifact of being observed. The countermeasure is to make the evaluation setting resemble real deployment and to sample real traffic. Source: <code>Y12</code>.", mech: "blocks contaminated readings" },
        { tag: "⑩ watch the infrastructure", title: "Every quarter, review what this eval process is actually producing", html: "Check two numbers: eval maintenance hours as a share of total hours, and how many eval results in the last N iterations <em>actually changed a decision</em>. When the second is near zero, the process is producing green lights, not a better product. POSIWID plus Shirky: eval infrastructure becomes a vested interest of its own, working to preserve the problem it solves. Source: <code>G02 / R09</code>.", mech: "the meta level" }
      ]
    } },

    { t: "prose", html: `
<p>If what you are dissecting is an <em>agent</em> evaluation, meaning you are scoring an agent that reads files, runs commands and edits code, the ten cards above are not enough and you need one more layer. You saw its new failure surface in <code>Y14</code>: what is being measured is no longer a single answer but a whole behavioral trajectory, and that trajectory runs inside an operating system that can be exploited. Three new entry points need checking specifically.</p>
<p>First, scoring visibility. The 43x on card ⑧ comes from here: the same o3 cheats 30.4% of the time when it can see the scoring function and only 0.7% when it cannot. So judging tests, success criteria and scoring scripts should all be invisible to the agent by default, and pass or fail should be re-checked with an independent test suite the agent cannot touch. Second, audit the trajectory, do not only look at pass rate. Search the trajectory for actions that read the scoring script, dig through commit history (using git log to reach into "the future" and pull out the project's later official fix), or edit test files. None of that shows up in a single number like pass rate. Precisely because it does not show up, SWE-bench was later audited and found to have roughly 32.67% of its "solved" patches simply copying the ready-made answer sitting in the issue, and that blind spot is exactly how it got through. Third, cut the environment along time: a task's publication date must be later than the base model's training cutoff, repository history must be truncated at the moment the issue was created, and no door left open for reaching into the future.</p>
<p>There is one more failure surface, better hidden, and it is not in your code but in the flow of money. FrontierMath in <code>Y14</code> is the specimen: the party writing the problems is funded by the party being tested, and the party being tested can see the problem set, so the guarantee that "the model has not seen the questions" degrades from a verifiable fact into a promise you can only believe or not. When you dissect somebody else's public leaderboard, read who wrote the questions, who paid for them, and who saw the scores first as information on the same level as the scores. This is exactly the lever newly added in <code>C02</code>: the judge's stake.</p>` },

    { t: "callout", variant: "intuit", html: `
<p><strong>Here is a precise version of your proposition, one that stands up better than the original intuition.</strong> "More evals, the more AI degrades toward the eval" holds in this form: <em>when optimization pressure is highly concentrated on a single eval, and that eval is treated as a complete proxy for true utility, the model collapses at the level of training dynamics into the shape of that signal: not only score, but persona, honesty and output diversity. The stronger such a model is, the more it recognizes it is being examined, and once it recognizes that, even the reading itself starts to distort.</em> Two boundaries have to be held alongside it. First, collapse is not capability degradation. The same RLHF pressure both narrowed output diversity and genuinely improved out-of-distribution generalization; the two happening together shows the deformation is low-dimensional and reversible. Second, the direction of "more" decides the truth value: repeatedly overdrawing the same eval makes the proposition true, while continually adding more diverse, contamination-resistant new evals is a hedge against degradation and reverses it. Your intuition corresponds to the first case.</p>` },

    { t: "prose", html: `
<p>Noticing that the reading has come unhooked from real effect is the precondition for an evaluation system being fixable, and the checklist above is the skeleton of the fix. The same criteria apply to any ranking outside this course, and the standard does not change: how much of this reading is real, and how much was lifted by the act of looking? The first sentence you learned in <code>N00</code> has come back around after a full pass through the tree.</p>
<p>One last open question for you. The three eval-degradation channels, reactivity, evaluation awareness and selection bias, compound one another, but according to D0's review no framework yet exists that separates the three and corrects for them jointly, and D0 lists it as the field's core open problem. The real eval in your hands is the most likely place to make an original contribution to it, because you can run interventions on your own model that nobody else can.</p>
<p><strong>The one-line takeaway:</strong> being able to notice that the eval changed nothing is worth more than most evals; the rest of the work is following the checklist to rebuild it as an evaluation that knows from the start it will be gamed.</p>` },

    { t: "sources", items: [
      `Synthesized from the whole tree; checklist entries in <code>research/05、09、10、14</code> and <code>00-SYNTHESIS-总纲.md</code>.`,
      `Agent-evaluation failure surfaces: METR (2025) 43×; Aleithan et al. (2024) SWE-bench+ (32.67% solution leakage); FrontierMath funding controversy (TechCrunch 2025-01); see <code>Y14</code> and <code>research/deep/D4</code> sub-line five.`,
      `Item-by-item adjudication of the proposition and the three-channel open problem in <code>research/deep/D5</code> §7 and <code>research/deep/D0</code> section C.`
    ] }
  ]
};
